# Import the modules you want to use


def ground_removal(points):
    """
    This function should implement ground removal from the input point cloud.

    :param points: A numpy array of shape (N, 3) where N is the number of points.
    :return: A numpy array of non-ground points of shape (M, 3) where M <= N.
    """
    # Replace this with the actual implementation.
    non_ground_points = points

    return non_ground_points
